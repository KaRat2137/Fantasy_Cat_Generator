{
  "name": "fantasy_cat_generator",
  "version": "1.0.0",
  "description": "Generate a unique fantasy cat character!",
  "main": "index.html",
  "scripts": {
    "build": "vite build && cp -r images dist",
    "deploy": "npm run build && firebase deploy",
    "dev": "vite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "KaRat",
  "license": "ISC",
  "dependencies": {
    "firebase": "^10.2.0",
    "html-to-image": "^1.11.11",
    "simple-loading-animation": "^0.2.0",
    "vite": "^4.4.9"
  }
}
