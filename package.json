{
  "name": "fantasy_cat_generator",
  "version": "1.0.0",
  "description": "Generate a unique fantasy cat character!",
  "main": "index.html",
  "scripts": {
    "build": "vite build",
    "deploy": "vite build && firebase deploy",
    "dev": "vite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "KaRat",
  "license": "ISC",
  "dependencies": {
    "firebase": "^10.2.0",
    "vite": "^4.4.9"
  }
}
